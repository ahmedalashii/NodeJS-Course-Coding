const sum = (a, b) => a + b;
const multiply = (a, b) => a * b;
const divide = (a, b) => a / b;
const subtract = (a, b) => a - b;
const PI = 3.14;
module.exports = { sum: sum, multi: multiply, divide: divide, subtract: subtract, pi: PI }; // key: value
// This is an object that contains all the functions and variables that we want to export