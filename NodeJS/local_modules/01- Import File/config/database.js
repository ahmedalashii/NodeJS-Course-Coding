const databaseConfig = {
    host: "localhost",
    driver: "mysql",
    database: "test",
    username: "root",
    password: "",
    port: 3306,
};

module.exports = databaseConfig; // here we export the databaseConfig object